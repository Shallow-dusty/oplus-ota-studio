<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App shell (Application, theme, MainActivity, navigation) lands in the
         app-shell commit. This manifest is intentionally minimal so the module
         assembles before any UI code exists. -->
    <application />
</manifest>
