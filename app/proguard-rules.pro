# App-specific R8 rules.
#
# Keep this file intentionally small. AndroidX, Compose, Room, WorkManager, and
# OkHttp provide their own consumer rules; add entries here only when release
# builds or install/run smoke reveals an app-owned reflection boundary.
